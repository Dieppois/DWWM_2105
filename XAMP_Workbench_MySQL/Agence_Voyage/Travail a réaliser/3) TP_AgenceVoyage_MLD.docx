PAYS = (country_code VARCHAR(2), country_name VARCHAR(128));
VOYAGES = (trip_code COUNTER, trip_title VARCHAR(128), trip_available INT, trip_start DATETIME, trip_end DATETIME, trip_price DECIMAL(15,2), trip_overview TEXT, trip_description TEXT);
THEMATIQUES = (theme_code COUNTER, theme_name VARCHAR(32), theme_description TEXT);
SERVICES = (service_code COUNTER, service_name VARCHAR(32), service_description TEXT, service_score BYTE, service_commentaire VARCHAR(255));
COMMERCIALS = (com_code_ VARCHAR(5), com_name VARCHAR(64), com_password VARCHAR(60), #com_code__peut_remplacer);
VILLES = (city_code COUNTER, city_name VARCHAR(128), #country_code);
ESCALES = (step_start DATETIME, step_end_ DATETIME, #city_code, #trip_code);
CLIENTS = (client_id COUNTER, client_lastname VARCHAR(32), client_email VARCHAR(128), client_firstname VARCHAR(32), client_phone CHAR(16), client_added DATE, client_password VARCHAR(60), #com_code_);
Proposer = (#trip_code, #theme_code);
Assurer = (#trip_code, #service_code);
Reserver = (#trip_code, #client_id, order_quantity INT, order_paid LOGICAL);

